To use sym-plot in ghci

> import Math.Sym.Plot
Math.Sym.Plot > import Math.Sym

For listing permutations

Math.Sym.Plot Math.Sym > perms 3 :: [String]

for listing permutations top line of two line notation

Math.Sym.Plot Math.Sym > perms 3 :: [StPerm]

for plotting single permutation

Math.Sym.Plot Math.Sym > savePNG 200 "perm.png" $ plotPerm "312"

for plotting multiple permutations 

Math.Sym.Plot Math.Sym > savePNG 600 "perms.png" $ plotPerms 3 (perms 3 :: [String])
